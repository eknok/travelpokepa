<script>
　function myfunc1() {
    var index = document.getElementById("selectbox1").selectedIndex;
    var value = document.getElementById("selectbox1").value;
    if (value >= 1 && value <= 9 ) {
      var select_spr = "00" + String(value)
    } else if (value >= 10 && value <= 99 ) {
      var select_spr = "0" + String(value)
    } else {
    var select_spr = String(value)
    }
    document.getElementById("image1").innerHTML = "<img src =/pokemon_images/spr/"+ select_spr +"MS.png>";
  }

  function myfunc2() {
  var index = document.getElementById("selectbox2").selectedIndex;
  var value = document.getElementById("selectbox2").value;
  if (value >= 1 && value <= 9 ) {
    var select_spr = "00" + String(value)
  } else if (value >= 10 && value <= 99 ) {
    var select_spr = "0" + String(value)
  } else {
  var select_spr = String(value)
  }
  document.getElementById("image2").innerHTML = "<img src =/pokemon_images/spr/"+ select_spr +"MS.png>";
}

function myfunc3() {
  var index = document.getElementById("selectbox3").selectedIndex;
  var value = document.getElementById("selectbox3").value;
  if (value >= 1 && value <= 9 ) {
    var select_spr = "00" + String(value)
  } else if (value >= 10 && value <= 99 ) {
    var select_spr = "0" + String(value)
  } else {
  var select_spr = String(value)
  }
  document.getElementById("image3").innerHTML = "<img src =/pokemon_images/spr/"+ select_spr +"MS.png>";
}

function myfunc4() {
  var index = document.getElementById("selectbox4").selectedIndex;
  var value = document.getElementById("selectbox4").value;
  if (value >= 1 && value <= 9 ) {
    var select_spr = "00" + String(value)
  } else if (value >= 10 && value <= 99 ) {
    var select_spr = "0" + String(value)
  } else {
  var select_spr = String(value)
  }
  document.getElementById("image4").innerHTML = "<img src =/pokemon_images/spr/"+ select_spr +"MS.png>";
}

function myfunc5() {
  var index = document.getElementById("selectbox5").selectedIndex;
  var value = document.getElementById("selectbox5").value;
  if (value >= 1 && value <= 9 ) {
    var select_spr = "00" + String(value)
  } else if (value >= 10 && value <= 99 ) {
    var select_spr = "0" + String(value)
  } else {
  var select_spr = String(value)
  }
  document.getElementById("image5").innerHTML = "<img src =/pokemon_images/spr/"+ select_spr +"MS.png>";
}

function myfunc6() {
  var index = document.getElementById("selectbox6").selectedIndex;
  var value = document.getElementById("selectbox6").value;
  if (value >= 1 && value <= 9 ) {
    var select_spr = "00" + String(value)
  } else if (value >= 10 && value <= 99 ) {
    var select_spr = "0" + String(value)
  } else {
  var select_spr = String(value)
  }
  document.getElementById("image6").innerHTML = "<img src =/pokemon_images/spr/"+ select_spr +"MS.png>";
}


</script>
